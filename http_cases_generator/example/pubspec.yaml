name: http_cases_example
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  http_cases:
    path: ../../http_cases

dev_dependencies:
  http_cases_generator:
    path: ..
  craft_runner:
    path: ../../craft_runner
  lints: ^5.0.0
  test: ^1.24.0

dependency_overrides:
  craft_runner:
    path: ../../craft_runner
